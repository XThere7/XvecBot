# Relative URL → same-origin via Vite proxy (works on localhost AND 192.168.x.x, no CORS).
# To bypass the proxy and hit the backend directly, use e.g. http://192.168.1.27:8000/api/v1
VITE_API_URL=/api/v1
VITE_API_KEY=your-secret-api-key-change-in-production
