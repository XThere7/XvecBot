# Relative URL → same-origin via Vite proxy (works on localhost AND any LAN IP, no CORS).
# For direct-backend access without proxy: VITE_API_URL=http://192.168.1.27:8000/api/v1
VITE_API_URL=/api/v1
VITE_API_KEY=your-secret-api-key-change-in-production
