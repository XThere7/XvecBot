# ── OpenVINO LLM backend (optional) ──────────────────────────────────────────
# Install ONLY if you use LLM_PROVIDER=openvino in .env:
#   pip install -r backend/requirements-openvino.txt
#
# NOTE: needs Python 3.10–3.13 (OpenVINO does not publish wheels for every
# brand-new Python release). If this repo's .venv is newer, create a separate
# venv with Python 3.12 for the OpenVINO backend.
#
# Versions are floors, not pins, so pip can resolve on your platform.
openvino>=2025.4
optimum-intel>=1.20
transformers>=4.45
huggingface-hub>=0.25
# nncf is ONLY needed if you quantize your own model to INT4 yourself.
# Skip it when using the pre-quantized OpenVINO/*-int4-ov models (recommended).
# nncf>=2.14
